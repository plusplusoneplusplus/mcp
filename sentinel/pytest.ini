[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: marks tests that interact with the real system
addopts =
    --verbose
    --cov=sentinel
    --cov-report=term-missing
    --cov-report=html
    --no-cov-on-fail
