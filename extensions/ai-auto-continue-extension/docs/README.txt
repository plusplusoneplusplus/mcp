This is a scaffold for a VS Code extension using TypeScript. No implementation is present yet.

To make this extension installable and runnable:
1. Run `npm install` in this directory to install dependencies.
2. Run `npm run compile` to build the extension.
3. Open this folder in VS Code and press F5 to launch an Extension Development Host.
